/**
 * Placeholder genre fetcher — returns empty array.
 * Used when genre enrichment is disabled.
 */
const fetchGenreFromOpenLibrary = async () => {
  return [];
};

export default fetchGenreFromOpenLibrary;